apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: asgardeo-secrets
  namespace: production
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets
    kind: ClusterSecretStore
  target:
    name: asgardeo-secrets
    creationPolicy: Owner
  data:
    - secretKey: ASGARDEO_CLIENT_SECRET
      remoteRef:
        key: hrms/asgardeo
        property: ASGARDEO_CLIENT_SECRET
