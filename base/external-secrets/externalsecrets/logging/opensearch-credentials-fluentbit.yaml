apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: opensearch-credentials
  namespace: logging
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets
    kind: ClusterSecretStore
  target:
    name: opensearch-credentials
  data:
    - secretKey: host
      remoteRef:
        key: opensearch
        property: host
