apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/part-of: hrms
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: hrms-gateway
      namespace: istio-system
  rules:
    - matches:
        - path:
            type: Exact
            value: "/api/v1/audit-logs"
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: "/api/v1/audit-logs/"
      backendRefs:
        - group: ""
          kind: Service
          name: audit-service
          port: 80
          weight: 1

    - matches:
        - path:
            type: PathPrefix
            value: "/api/v1/audit-logs"
      backendRefs:
        - group: ""
          kind: Service
          name: audit-service
          port: 80
          weight: 1
