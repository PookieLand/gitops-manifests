apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: leave-service
  labels:
    app.kubernetes.io/name: leave-service
    app.kubernetes.io/part-of: hrms
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: hrms-gateway
      namespace: istio-system
  rules:
    - matches:
        - path:
            type: Exact
            value: "/api/v1/leaves"
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: "/api/v1/leaves/"
      backendRefs:
        - group: ""
          kind: Service
          name: leave-service
          port: 80
          weight: 1

    - matches:
        - path:
            type: PathPrefix
            value: "/api/v1/leaves"
      backendRefs:
        - group: ""
          kind: Service
          name: leave-service
          port: 80
          weight: 1
