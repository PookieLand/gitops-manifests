apiVersion: v1
kind: Secret
metadata:
  name: fluent-bit-opensearch-secret
  namespace: logging
stringData:
  OPENSEARCH_USER: "<replace-with-username>"
  OPENSEARCH_PASS: "<replace-with-password>"

# Replace stringData values with your OpenSearch credentials before applying.
