apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: redis-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://charts.bitnami.com/bitnami'
    chart: redis
    targetRevision: 19.0.0
    helm:
      valueFiles:
        - ../../base/services/redis-service/values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: redis-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
